mainmenu "kabot"

# Source the main Zephyr Kconfig tree
source "Kconfig.zephyr"

# Keep symbol definitions in Kconfig.module to avoid duplication with module/compliance entrypoints.
rsource "Kconfig.module"
